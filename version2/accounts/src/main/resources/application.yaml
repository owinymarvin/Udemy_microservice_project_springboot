server:
  port: 8080


build:
  version: "3.0"


spring:
  application:
    name: "accounts"
  profiles:
    active: "prod"
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import: "optional:configserver:http://localhost:8071"
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"


accounts:
  message: "Welcome to the Bank App. default profile."
  contactDetails:
    name: "Owiny Marvin - Developer"
    email: "marvin4oliveeb@gmail.com"
  onCallSupport:
    -(555) 555-1234
    -(555) 523-1345


management:
  endpoints:
    web:
      exposure:
        include: "*"
